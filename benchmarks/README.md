# Benchmarks

Phase 8 will add reproducible CPU-first parsing, construction, event-site,
filter/smoother, hyperparameter, outer-iteration, prediction, serialization,
peak-memory, and scaling benchmarks. Results will record hardware and versions.

